global:
  serverConnector:
    addresses:
      - ${POLARIS_SERVER}:8091
config:
  configConnector:
    addresses:
      - ${POLARIS_SERVER}:8093
  configFilter:
    enable: true
    chain:
      - crypto
    plugin:
      crypto:
        entries:
          - name: AES
