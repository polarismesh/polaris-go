global:
  serverConnector:
    addresses:
      - ${POLARIS_SERVER}:8091
  client:
    labels:
      env: pre
config:
  configConnector:
    addresses:
      - ${POLARIS_SERVER}:8093
    token: ${POLARIS_TOKEN}
