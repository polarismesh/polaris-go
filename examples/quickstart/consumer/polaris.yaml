global:
  serverConnector:
    addresses:
      - 127.0.0.1:8091
  statReporter:
    enable: false
    chain:
      - prometheus
      # - pushgateway
    plugin:
      prometheus:
        type: pull
        metricPort: 0
