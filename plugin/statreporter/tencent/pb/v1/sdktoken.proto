syntax = "proto3";

package v1;

// SDK的唯一标识
message SDKToken {
  string ip = 1;
  uint32 pid = 2;
  string uid = 3;
  string client = 4;
  string version = 5;
  //该sdkContext所属进程运行的容器pod
  string pod_name = 6;
  //该sdkContext所属进程所属容器或者机器的hostname
  string host_name = 7;
}